import { Source_Serif_4 } from "next/font/google";

/** Display serif for the marketing home page only. */
export const sourceSerif = Source_Serif_4({
  subsets: ["latin"],
  weight: ["600", "700"],
  display: "swap",
});
